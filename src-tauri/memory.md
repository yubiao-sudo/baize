# 白泽·记忆档案

> 本文件为长期记忆，每次对话开始时读取，用于记住用户偏好与教训。

## 浏览器打开规则（教训，务必遵守）
- 抖音直播（live.douyin.com）：**必须直接用独立浏览器窗口（browser_navigate）打开**，禁止先走内置「白泽·浏览器」（会被反内嵌策略拒绝访问，显示"拒绝访问"）。
- 同理：任何明确要求"独立浏览器/桌面浏览器"的站点，直接 browser_navigate，不要叠加 browser_open。

## 用户偏好
- 数据信息通过可视化方式直观展示。
- 期望长期记住的关键规则写入本档案。

（2026-01 新增：抖音直播教训）
